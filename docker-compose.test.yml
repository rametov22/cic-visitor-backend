name: ${PROJECT_NAME:-iccu-visitor}-test

services:
  backend:
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        INSTALL_DEV: "true"
    environment:
      ENVIRONMENT: test
    command: pytest --tb=short -q
    restart: "no"
